<?php

declare(strict_types=1);

/**
 * @var int $statusCode
 * @var string $message
 * @var string $errorId
 * @var string|null $supportEmail
 * @var callable $e
 */

?>
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error <?= $e($statusCode) ?></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 24px;
            background: #0c0c0e;
            color: #e4e4e7;
            font: 16px/1.5 system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        main {
            width: min(100%, 500px);
            text-align: center;
        }

        .status {
            color: #ef4444;
            font-size: clamp(64px, 14vw, 96px);
            font-weight: 700;
            line-height: 1;
        }

        .message {
            margin-top: 16px;
            color: #a1a1aa;
            font-size: 20px;
        }

        .error-id,
        .support {
            margin-top: 12px;
            color: #71717a;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
            font-size: 14px;
        }

        a {
            color: #93c5fd;
        }
    </style>
</head>
<body>
<main>
    <div class="status"><?= $e($statusCode) ?></div>
    <p class="message"><?= $e($message) ?></p>
    <p class="error-id">Error ID: <?= $e($errorId) ?></p>
    <?php if ($supportEmail !== null): ?>
        <p class="support">Contact support: <a href="mailto:<?= $e($supportEmail) ?>"><?= $e($supportEmail) ?></a></p>
    <?php endif; ?>
</main>
</body>
</html>
